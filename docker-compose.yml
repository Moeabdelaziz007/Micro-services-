version: '3.8'
services:
  ui-agent:
    build: ./agents/ui-agent
    ports:
      - "3001:3000"
  db-agent:
    build: ./agents/db-agent
  devops-agent:
    build: ./agents/devops-agent
  research-agent:
    build: ./agents/research-agent
networks:
  default:
    name: microservices-network
